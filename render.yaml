services:
  # Backend API
  - type: web
    name: belgrade-mama-api
    env: node
    region: frankfurt
    plan: free
    buildCommand: cd apps/api && npm install && npx prisma generate
    startCommand: cd apps/api && npx prisma migrate deploy && npm run start
    envVars:
      - key: DATABASE_URL
        value: postgresql://neondb_owner:npg_bKCHnzQ75xrI@ep-damp-surf-agvnau3d-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require
      - key: JWT_SECRET
        value: SX24LyfnbnhB036bgthpv2fwzAfjcSzjHOhBCUx07D4=
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: https://belgrade-mama-web.onrender.com

  # Frontend Static Site
  - type: web
    name: belgrade-mama-web
    env: static
    region: frankfurt
    plan: free
    buildCommand: cd apps/web && npm install && npm run build
    staticPublishPath: apps/web/dist
    envVars:
      - key: VITE_API_URL
        value: https://belgrade-mama-api.onrender.com
